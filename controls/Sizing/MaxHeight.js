const MaxHeight = [{
  title: "Max Height",
  id: "globalMaxHeight",
  format: "max-h-{{value}}",
  themeSpacing: {
    mode: "single",
    default: {
      base: {
        custom: false,
        value: "auto"
      }
    }
  }
}
];

export default MaxHeight;
